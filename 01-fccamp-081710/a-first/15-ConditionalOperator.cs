public class ConditionalOperator
{
  public void runConditionalOperator()
  {
    int age = -10;

    // condition ? true : false;
    string result = (age >= 0) ? "Valid" : "Invalid";

    Console.WriteLine(result);
  }
}
