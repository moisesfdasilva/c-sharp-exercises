namespace InitialLessonsB
{
  class Program
  {
    static void Main(string[] args)
    {
      // VerbatimStringLiteral verbatimStringLiteral = new VerbatimStringLiteral();
      // verbatimStringLiteral.runVerbatimStringLiteral();

      // StringFormatting stringFormatting = new StringFormatting();
      // stringFormatting.runStringFormatting();
      
      // StringInterpolation stringInterpolation = new StringInterpolation();
      // stringInterpolation.runStringInterpolation();

      // StringConcatenation stringConcatenation = new StringConcatenation();
      // stringConcatenation.runStringConcatenation();

      // EmptyString emptyString = new EmptyString();
      // emptyString.runEmptyString();

      // StringEqualsFunction stringEqualsFunction = new StringEqualsFunction();
      // stringEqualsFunction.runStringEqualsFunction();

      // StringIteration stringIteration = new StringIteration();
      // stringIteration.runStringIteration();

      // IsNullOrEmpty isNullOrEmpty = new IsNullOrEmpty();
      // isNullOrEmpty.runIsNullOrEmpty();

      // StringInReverse stringInReverse = new StringInReverse();
      // stringInReverse.runStringInReverse();

      // PasswordChecker passwordChecker = new PasswordChecker();
      // passwordChecker.runPasswordChecker();

      // Arrays arrays = new Arrays();
      // arrays.runArrays();

      // ArraySorting arraySorting = new ArraySorting();
      // arraySorting.runArraySorting();

      // ArrayReversal arrayReversal = new ArrayReversal();
      // arrayReversal.runArrayReversal();

      // ArrayClearing arrayClearing = new ArrayClearing();
      // arrayClearing.runArrayClearing();

      // ArrayIndexOf arrayIndexOf = new ArrayIndexOf();
      // arrayIndexOf.runArrayIndexOf();

      // Lists lists = new Lists();
      // lists.runLists();

      // Dictionaries dictionaries = new Dictionaries();
      // dictionaries.runDictionaries();

      // OddEvenNumbers oddEvenNumbers = new OddEvenNumbers();
      // oddEvenNumbers.runOddEvenNumbers();

      ArrayOfMultiples arrayOfMultiples = new ArrayOfMultiples();
      arrayOfMultiples.runArrayOfMultiples();

      //https://www.youtube.com/watch?v=YrtFtdTTfv0 [4:30:00]
    }
  }
}