namespace InitialLessons
{
  //https://www.youtube.com/watch?v=YrtFtdTTfv0
  class Program
  {
    static void Main(string[] args)
    {
      // Numbers numbers = new Numbers();
      // numbers.runNumbers();

      // Strings strings = new Strings();
      // strings.runStrings();

      // Bools bools = new Bools();
      // bools.runBools();

      // Operations operations = new Operations();
      // operations.runOperations();

      // Modulus modulus = new Modulus();
      // modulus.runModulus();

      // VarKeyword varKeyword = new VarKeyword();
      // varKeyword.runVarKeyword();

      // ConstKeyword constKeyword = new ConstKeyword();
      // constKeyword.runConstKeyword();

      // StoringUserData storingUserData = new StoringUserData();
      // storingUserData.runStoringUserData();

      // Odd odd = new Odd();
      // odd.runOdd();

      // ConsoleIO consoleIO = new ConsoleIO();
      // consoleIO.runConsoleIO();

      // IfStatements ifStatements = new IfStatements();
      // ifStatements.runIfStatements();

      // SwitchStatements switchStatements = new SwitchStatements();
      // switchStatements.runSwitchStatements();

      // ForLoops forLoops = new ForLoops();
      // forLoops.runForLoops();

      // WhileLoops whileLoops = new WhileLoops();
      // whileLoops.runWhileLoops();

      // ConditionalOperator conditionalOperator = new ConditionalOperator();
      // conditionalOperator.runConditionalOperator();

      // NumericFormatting numericFormatting = new NumericFormatting();
      // numericFormatting.runNumericFormatting();

      // TryParseFunction tryParseFunction = new TryParseFunction();
      // tryParseFunction.runTryParseFunction();

      // TimesTable timesTable = new TimesTable();
      // timesTable.runTimesTable();

      FizBuzzGame fizBuzzGame = new FizBuzzGame();
      fizBuzzGame.runFizBuzzGame();
    }
  }
}